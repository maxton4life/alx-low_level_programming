#include <stdio.h>
/**
 *main - entry point
 *
 *Description:prints the alphabet
 *
 *Return: Always 0 (successful)
 */
int main(void)
{
for (ch = 'a'; ch <= 'z' ; ch++;)
{
putchar(ch);
}
return (0);
}
